name: Test Tailscale

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Install Tailscale
        run: |
          curl -fsSL https://tailscale.com/install.sh | sh
      
      - name: Connect (exact copy of successful test)
        env:
          TS_AUTHKEY: ${{ secrets.TS_AUTHKEY }}
        run: |
          tailscale up --authkey="$TS_AUTHKEY" --hostname=test-gh --accept-dns=false --accept-routes=false --verbose=2 2>&1
          echo "Exit code: $?"
          tailscale status 2>&1
